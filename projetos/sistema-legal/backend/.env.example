# Copiar para .env e ajustar antes de produção:
#   cp .env.example .env

# Obrigatório em produção — mínimo 32 caracteres aleatórios
JWT_SECRET=alterar-para-segredo-forte-em-producao-min-32-chars

# Porta do servidor (Railway/Render definem PORT automaticamente)
PORT=3001
